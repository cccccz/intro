假页面，不是产品规格。规格以仓库根目录 `结论.md` 为准；实现顺序见 `functional_milestones.md`。

文件：`mockup/delta-hedging.html`（自包含 HTML + KaTeX CDN）。无存储、无 Electron、无正文标记语法。演示用的 delta-hedging 材料是写死的。顶栏「新建 / 复用 / 节选」只弹出说明，不写入。

演示了且与结论一致的：列=深度、同层侧边共一列、默认同时几列可改、置顶钉选区源文（含 TeX 再渲染）、视口外源高亮则不画对应侧边（不是合上）。

三种看法（结论第 8 条）在顶栏可切：大纲树、缩略卡片、色块地图。地图等大色块、空格表示宿主正文距离、悬停出标题、**不**点格跳转。树和缩略图在假页面里仍可点回列，那是演示方便，不是色块地图的交互。

不要当规格的：可拖分割条、假右键菜单、图用文字占位、节点–边「图谱」（已弃）、未完成的 `content.ts` 抽取。预埋的 `<mark>` 和缩略网格只是演示，不是产品写路径。标记格式仍未选定（结论第 35 条）。

正文铆点高亮与列间导线已升为写者壳产品可见反馈（结论第 39 条）。假页面里的画法仍是演示，真实现在 `app/electron/renderer/`。
