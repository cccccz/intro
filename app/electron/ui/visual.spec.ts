import { test } from "@playwright/test";

/**
 * P2 placeholder. `npm run test:ui` does not include this project.
 * Baselines belong in .ui-baselines (gitignored). Do not git add screenshots.
 */
test.describe("visual baselines", () => {
  test.skip("placeholder: empty chrome screenshot stays local", () => {});
});
