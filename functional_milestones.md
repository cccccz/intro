顺序：文本 → 图 → 搜索/复用 → 导出 → PDF。里程碑按功能支持划分，主轴是介质能力。

M1:
1. 宿主和笔记都是文本；公式能渲染。常见宿主是推导，数学渲染放在这一档。
2. 最小工作区回路：划选、新建侧边、侧边再挂侧边、开合。列按深度，同层侧边共一列。第一版不能只是文本编辑器。
3. 此阶段只要树结构。第 8 条的三种看法不单独占里程碑，随时可加，且不应挡住后面的 PDF。色块地图不是导航。
4. 置顶可放在本档末尾或紧随其后：人为把选区钉进上方矮的横向笔记带。对证明来说，置顶比缩略图更有用。

M2:
1. 笔记里能插图、能显示图，先当附件。
2. 图上划区锚定不做，以免和 PDF 选区保真度缠在一起。

M3:
1. 搜索、复用已有笔记、节选。库里不止几条笔记时才需要。此阶段仍不做 PDF。

M4:
1. 可再打开的工作区包，以及 HTML 渲染件；披露范围由笔者选择。

M5:
1. PDF 作宿主，高亮保真，侧边挂在页上选区。单独一档。
2. 图上精确划区锚定归入选区保真度，跟本档一起做，不放进 M2。
