# intro 工作约定

先读 [`docs/ENGINEERING.md`](docs/ENGINEERING.md)。[`docs/history/`](docs/history/) 里是过时记录，不是当前入口。

- 不要把新的界面建议自动做成必须先做完的功能清单。
- 正式资料库（包括 `C:\Users\Zheng\Desktop\paul`）不是测试数据。自动化写入测试使用临时库或副本，不修改正式库，除非用户明确要求该次操作。
- 后续版本须兼容现有 library 的正文、标题、稳定 ID、引用关系、PDF 和元数据。迁移先备份、在副本验证、输出新目录，不静默原地升级。
- 不要两个进程同时编辑同一个库。当前没有跨进程编辑锁或冲突合并。
- 涉及写回和存储的变更必须有旧库兼容回归。不要向仓库提交真实学习库或其备份。
