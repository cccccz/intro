# 学习使用版与后续维护

2026-09-05 起，intro 的首要目标是支持日常学习。当前功能已经达到用户认可的可用程度，开始使用不等待功能清单完成。

## 本次封存

- 封存当前 `9615918`（含置顶）的应用代码，标识为 `study-2026-09-05-9615918`。已有 `v0.1.0` 指向更早的 `7217f61`，保留不移动。
- 使用版是独立的 Windows 程序目录和桌面入口，不执行 `npm run build:app`，不引用开发目录中的代码或运行时。开发不覆盖这个目录。
- 使用版与开发版使用独立的应用配置目录。PDF、笔记、锚点仍保存在用户的 library 中，不搬进程序目录。
- 本次封存是可用快照，不宣称没有缺陷，也不自动更新。以后是否换版由用户决定；旧版保留。

本机入口及产物：

- 桌面 `intro-study.lnk`：打开正式 `paul` 库。
- 使用版：`C:\Users\Zheng\Documents\intro-releases\study-2026-09-05-9615918\intro.exe`。
- 使用版配置：上述目录的 `profile\`，与开发配置分离。已经复制封存时的原应用配置；后续两者各自保存。
- 封存前备份：`C:\Users\Zheng\Documents\intro-backups\20260905-200726-before-study\`，含 `paul\` 和 `app-profile\`。这是一份本次快照，不是自动持续备份。
- 使用版目录含 `release.json`（版本、依赖版本、应用文件 SHA-256、备份位置）和 `source-9615918.zip`（对应源代码）；源码依赖锁另存为 `source-package-lock.json`。
- 收尾验证：106 项测试、类型检查和构建通过；独立程序在 paul 副本中加载 1401 页 PDF 成功，最终程序从独立目录启动并载入正式库。

打包使用 Electron 自带运行时的 `resources/app` 目录布局；仅启动包装层设置独立配置路径及单实例锁，业务代码保持该提交内容。参考 [Electron 应用分发](https://github.com/electron/electron/blob/main/docs/tutorial/application-distribution.md) 和 [app.setPath](https://www.electronjs.org/docs/latest/api/app#appsetpathname-path)。

## 现在即可开始学习

已实际验证 paul 的 PDF 阅读、已有侧注逐层展开、目录显示、远距离跳页；临时库验证了创建笔记、公式渲染、侧注、置顶及置顶写回。布局、搜索、图片、分享、AI、聚焦分支均不阻挡封存。

**已知优先维护缺陷：普通正文编辑会沿用旧字符位置，可能使已有文本锚点错位或消失。** 当前快照中，已经挂有子侧注的文本篇，暂不直接修改其正文；可继续读、展开、写尚无子锚点的笔记。PDF 锚点采用侧车坐标，不受这个字符位移问题影响。置顶写回有范围校验，但不是完整的通用编辑修复。已有错位不会因未来修复自动复原。

此问题列为下一次数据可靠性补丁的首项，不与其他功能捆绑。修复必须覆盖开头插字、删除、选区内修改、重复文本、嵌套锚点及多处编辑；不确定的映射不得静默错挂。不能以持续追加功能推迟这项修复。

## 使用与开发隔离

1. `C:\Users\Zheng\Desktop\paul` 是正式学习库，后续开发和自动化写入测试默认禁止使用它。
2. 开发使用新建临时库、合成测试库，或正式库的完整副本。测试用副本不得反向覆盖正式库。
3. 正式库的正文、PDF、锚点、稳定 ID 与未知元数据都属于用户数据；不得为了方便新实现而重置、清理或重新导入。
4. 不同时用使用版和开发版编辑同一个库。当前没有跨进程编辑锁或冲突合并能力。
5. 置顶和阅读进度目前还在应用配置中。只备份 library 不包含这些状态；备份时一并保存使用版配置，且先关闭应用。

## 每次升级的最低门槛

- 打开旧库必须兼容；不需要迁移时，打开行为不得重写库文件。
- 升级前关闭使用版，完整备份 library 和使用版配置；先在副本上验证打开、保存、重开、侧注及 PDF。
- 修改写回或存储代码时，加入旧格式兼容回归，并检查正文、标题、ID、引用关系及 PDF 字节。
- 尽量不迁移。确需改变格式时，明确格式版本、变更说明和独立迁移步骤；输出新目录，不原地覆盖旧库。
- 旧程序不一定能读新版库。回退必须使用对应的旧版程序与升级前的旧库副本，不能默认旧程序可以打开迁移后的库。
- 用户确认换版之前，不更新桌面学习入口、不替换使用版、不迁移正式库。

## 后续需求进入开发的条件

学习中遇到的问题先记下，不当场中断学习去开发。优先处理数据损失、错挂、打不开、无法保存及反复阻断学习的问题；偶发不便与新想法留在待办。每次维护只解决明确的小范围问题，验证后停止。

成功标准是能够连续学习并留下可恢复的笔记，不是功能数量。

## 下一版待发布（封版时间待定）

- 用户在实际学习中提出侧边 Pin：来源滚出视口时，保留手动固定的侧注。开发分支为 `codex/side-pin`，规则见产品结论第 42 条。
- 此功能只新增显示偏好，不改变 library 格式，不替换本次封存快照或桌面入口。文本锚点编辑可靠性缺陷仍是已知优先维护事项，并未因增加 Pin 而修复。

## 2026-09-05 第二次学习版：数学兼容与 Side Pin

用户已要求换版，桌面 intro-study.lnk 现指向 C:\Users\Zheng\Documents\intro-releases\study-2026-09-05-math-pin\intro.exe。原学习版保留。

新增原生识别 `\[...\]` 独立公式和 `\(...\)` 行内公式，保留 `$` / `$$`；只改变渲染，不改写 library。前述 Side Pin 已随本版发布。110 项测试、类型检查、构建及浏览器真实 KaTeX / Pin 检查通过；按用户要求，桌面人工验收由用户进行。

换版前备份：C:\Users\Zheng\Documents\intro-backups\20260905-205615-before-math-pin，包含 paul、应用配置和原快捷方式；库文件逐一校验一致。新程序复制原配置以保留阅读状态，不迁移库格式。已有正文锚点编辑缺陷仍未修复。

## 2026-09-05 第三次学习版：公式内部标注

用户要求实现并替换桌面学习版。新目录：C:\Users\Zheng\Documents\intro-releases\study-2026-09-05-math-anchors。旧版并排保留。

- 公式中的锚点占位符在进入 KaTeX 前移除，显示层按解析器源码位置局部高亮；原有内部 side 不需迁移。
- 支持 Rendered 中选择数学项后 New side / Hang existing。重复符号按源码位置区分；多个挂接共用元素时提供选择菜单。无法映射的旧标注明确提示到 Source 检查。
- KaTeX 固定为 0.16.47，构建阶段通过带版本和匹配校验的 adapter 添加源码位置属性，保留默认 trust=false，不向 TeX 注入 HTML 命令。
- 正文保存通过 beforeinput 记录的选区和编辑序列更新锚点，保存前核对原文。普通前后编辑和标注内部替换保留关联。
- 删除整个来源或跨越挂接边界时拒绝覆盖保存，提示先撤销再解除挂接；不自动猜测或丢弃 side。未保存内容保留为按库/篇隔离的本地草稿，重开后可选择恢复。此版未实现“来源已删除”独立状态及自动重新挂接。
- 升级前备份：C:\Users\Zheng\Documents\intro-backups\20260905-212211-before-math-anchors（paul、应用配置、旧快捷方式）。不改写正式 library。
- 验证：118 项测试、类型检查和构建通过；浏览器实际公式选区→创建 side→高亮/连线→重开→编辑保存/拒绝删除/恢复草稿通过；旧 paul 副本 10 个公式无错误，编辑和重开保留关联，其他文件及 PDF 不变。桌面人工验收由用户执行。

## 2026-09-05 第四次学习版：合并数学标注高亮

桌面入口更新为 C:\Users\Zheng\Documents\intro-releases\study-2026-09-05-math-regions\intro.exe。Rendered 同一锚点的相邻数学元素合并为连续背景，悬停只显示区域外框；跨行和远离的区域不合并。独立 SVG 背景不改变数学排版、不拦截选区和点击；连线采用合并后的区域。字体加载、窗口大小和滚动变化时重算。Source、库格式和已有 side 保持不变。

119 项测试、类型检查、构建、真实公式合并高亮、悬停进入/移出、缩放对齐及完整 side 操作验证通过。备份目录：C:\Users\Zheng\Documents\intro-backups\20260905-225652-before-math-regions（paul、应用配置、旧桌面入口）。旧程序保留。
